# Tool payload

Release builds package the verified add-on as:

`renodx-dlss5-super-anus.addon64`

Maintainers must place that file here before running `scripts/publish.ps1`.
The binary is ignored by Git and validated against the pinned SHA-256 before
packaging. NVIDIA DLLs never belong in this folder.

Manager v1.0.2 includes addon build `1.0.3-pass-controls.4`, Windows file version
`1.0.3.4`, based on Preview 3 with persistent pass-section states and independent
transfer, colour and sharpness at all DX12 NR resolutions. The nested-SR guard
is retained; investigation-only probes are excluded. BOTDW's focus-change freeze
remains unresolved.
SHA-256: `4213C8D2C820891448A33225C6E1EA592F760194FFFE5A5C167FC2C3CD54B9FD`.
